# Base URL of the booking-api backend (server-side only)
API_URL="http://localhost:3001"
# Slug of the restaurant this site serves
RESTAURANT_SLUG="the-golden-fork"
# Secret that encrypts the NextAuth session cookie. Generate with: npx auth secret
AUTH_SECRET="generate-me"
